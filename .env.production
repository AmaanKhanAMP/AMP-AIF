# Used by `next build` / Vercel production builds (NEXT_PUBLIC_* are inlined at build time).
# You can also set these in the Vercel project Environment Variables UI.
NEXT_PUBLIC_API_URL=https://aif-backend-6jwe.onrender.com
