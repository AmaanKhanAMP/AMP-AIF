import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Archived Vite config — kept for reference during Next.js migration.
// Active tooling is Next.js (see package.json scripts).
export default defineConfig({
  plugins: [react()],
})
